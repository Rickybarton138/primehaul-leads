# Database (Required)
DATABASE_URL=postgresql://user:password@localhost/primehaul_leads

# Security (Required)
JWT_SECRET_KEY=change-me-to-a-64-char-random-secret

# OpenAI (Required for AI photo analysis)
OPENAI_API_KEY=sk-...
OPENAI_VISION_MODEL=gpt-4o-mini

# Mapbox (Required for maps)
MAPBOX_ACCESS_TOKEN=pk.eyJ...

# Stripe (Required for lead purchases)
STRIPE_SECRET_KEY=sk_test_...
STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Application
APP_ENV=development
APP_URL=http://localhost:8000

# Email (SMTP for notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=leads@primehaul.co.uk
SMTP_FROM_NAME=PrimeHaul Leads
